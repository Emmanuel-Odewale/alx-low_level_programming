#include <string.h>
#include "main.h"

/**
 * _strlen - a function that returns the length of a string.
 * @s: string to count
 * Return: returns length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
